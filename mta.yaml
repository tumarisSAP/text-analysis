ID: TextAnalysis
_schema-version: '2.1'
version: 0.0.1

modules:
 - name: TA
   type: hdb
   path: TA
   requires:
    - name: hdi_TA

resources:
 - name: hdi_TA
   properties:
      hdi-container-name: ${service-name}
   type: com.sap.xs.hdi-container
